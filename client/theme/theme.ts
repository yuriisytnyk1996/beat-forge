import { createTheme } from "@rneui/themed";

export const theme = createTheme({
  lightColors: {
    primary: "#e7e7e8",
    black_50: "#2d2d2d",
  },
  darkColors: {
    primary: "#000",
  },
  mode: "light",
});
